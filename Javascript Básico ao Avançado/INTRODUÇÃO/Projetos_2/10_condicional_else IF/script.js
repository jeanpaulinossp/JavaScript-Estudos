let nome = "Jean";

if (nome != undefined && nome == "João") {
    console.log ("Nome está definido");

} else if (nome == "Jean" && nome.length >= 4) {
    console.log ("Seu nome é Jean");
    
}    else {
    console.log ("Seu nome não é Jean!");
}
