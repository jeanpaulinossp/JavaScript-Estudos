const nome = prompt ("Digite o seu nome:");

if (nome == "Jean" || nome == "jean") {
    console.log (`Bem vindo ${nome}, sua presença é uma honra para nós!`); 

} else {
    console.log (`${nome}, seu acesso foi negado!`);
} 