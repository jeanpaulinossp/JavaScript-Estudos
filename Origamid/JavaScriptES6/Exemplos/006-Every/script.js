const numeros = [6, 43, 22, 88, 101, 29];

const every = numeros.every((n) => n > 3);

const every2 = numeros.every((n) => n > 7);

console.log(every);
console.log(every2);

// verifica dentro das condições passadas se todas as condições retornam true, caso contrário retornata false
